/** @type {import('next').NextConfig} */
const nextConfig = {
    // ако ползваш /dev/bridge:
    basePath: '/dev/bridge',
    assetPrefix: '/dev/bridge',
  
    reactStrictMode: true,
  };
  
  module.exports = nextConfig;